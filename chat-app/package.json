{
  "name": "elektra-chat-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Elektra Motors custom MIAW (Messaging for In-App and Web) chat UI — React SPA that talks directly to the Salesforce Messaging REST API and passes deviceId + vehicle context as routingAttributes to the Agentforce agent for real-time Data Cloud identity resolution.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "event-source-polyfill": "^1.0.31",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/event-source-polyfill": "^1.0.5",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.0"
  }
}
